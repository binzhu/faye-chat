<%= form_for @chatter, :remote => true do |f| %>
  <% if @chatter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chatter.errors.count, "error") %> prohibited this chatter from being saved:</h2>

      <ul>
      <% @chatter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<%=f.hidden_field :user, :value=>"me"%>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_field :message %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
